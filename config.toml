# The URL the site will be built for
base_url = "https://binrw.rs"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "juice"

title = "binrw"

[markdown]
highlight_code = true
highlight_theme = "ayu-dark"

[extra]
juice_logo_name = "binrw"
juice_logo_path = "binrw.svg"
juice_extra_menu = [
    { title = "Documentation", link = "https://docs.rs/binrw" },
    { title = "Github", link = "https://github.com/jam1garner/binrw"},
]
